{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "( npx prisma migrate deploy && ( timeout 120 npx prisma db seed || echo 'seed skipped (non-fatal)' ) || echo 'migrate/seed deferred — will retry on next deploy' ) & npm run start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
